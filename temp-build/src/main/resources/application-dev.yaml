spring:
  config:
    activate:
      on-profile: dev
  datasource:
    url: ${DB_URL:jdbc:postgresql://dpg-d7bq5btm5p6s73f3fg9g-a.oregon-postgres.render.com:5432/blog_alqc}
    username: ${DB_USERNAME:blog_alqc_user}
    password: ${DB_PASSWORD:hmuiD8Umupcc8o9DD03IhgcHLWtUuh83}
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  flyway:
    enabled: true
    baseline-on-migrate: true
  mail:
    host: ${MAIL_HOST:localhost}
    port: ${MAIL_PORT:1025}
    username: ${MAIL_USERNAME:no-reply@blog.local}
    password: ${MAIL_PASSWORD:}
    properties:
      mail:
        smtp:
          auth: ${MAIL_SMTP_AUTH:false}
          starttls:
            enable: ${MAIL_STARTTLS_ENABLE:false}

jwt:
  secret: ${JWT_SECRET:VGhpc0lzQURldmVsb3BtZW50S2V5Rm9yQmxvZ0FwcGxpY2F0aW9uMTIzNDU2Nzg5MDEyMzQ1Njc4OTA=}
  expiration: ${JWT_EXPIRATION:1800000}
  refresh-expiration: ${JWT_REFRESH_EXPIRATION:86400000}

app:
  frontend-url: ${FRONTEND_URL:https://plix-blog.vercel.app,https://plix-blog-git-*.vercel.app,http://localhost:3000}
  mail:
    from: ${MAIL_FROM:no-reply@blog.local}

management:
  health:
    mail:
      enabled: false
